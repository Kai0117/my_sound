<!DOCTYPE html>
<html>
  <head>
    <title>MindSound</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_pack_tag "react_entry", defer: true %>
  </head>

  <body class="bg-gradient-to-br from-slate-800 to-slate-900 text-white min-h-screen flex flex-col items-center justify-center px-4">
  <main class="flex flex-col items-center justify-center min-h-screen w-full">
      <%= yield %>
    </main>
  </body>
</html>

